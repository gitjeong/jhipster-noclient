apply plugin: "war"

bootWar {
    mainClass = "io.purple.jhipsternoclient.JhipsternoclientApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
